from collections import Counter

class Month_Cnt_class:
    def month_cnt_0(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[0]

    def month_cnt_1(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[1]

    def month_cnt_2(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[2]

    def month_cnt_3(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[3]

    def month_cnt_4(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[4]

    def month_cnt_5(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[5]

    def month_cnt_6(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[6]

    def month_cnt_7(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[7]

    def month_cnt_8(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[8]

    def month_cnt_9(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[9]

    def month_cnt_10(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[10]

    def month_cnt_11(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[11]

    def month_cnt_12(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[12]

    def month_cnt_13(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[13]

    def month_cnt_14(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[14]

    def month_cnt_15(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[15]

    def month_cnt_16(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[16]

    def month_cnt_17(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[17]

    def month_cnt_18(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[18]

    def month_cnt_19(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[19]

    def month_cnt_20(arr):
        cnt_pairs = Counter(arr)
        return cnt_pairs[20]